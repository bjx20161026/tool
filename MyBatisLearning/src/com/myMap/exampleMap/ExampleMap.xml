<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myMap.exampleMap.ExampleMap">
	<!-- <select id="selectBlog" resultType="Blog"> select * from Blog where 
		id = #{id} </select> -->
	<select id="selectExample" resultType="int">
		select 1 from dual
	</select>
	<select id="selectWorkOrder" resultType="WorkOrder">
		select * from work_order where WORKJOB_ID = #{id}
	</select>
	<select id="selectMap" resultType="hashmap">
		select * from work_order
	</select>
</mapper>